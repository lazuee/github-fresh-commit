name: Fresh Commit
on: workflow_dispatch

jobs:
    commit:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - uses: ./
              with:
                  github_token: ${{ secrets.GITHUB_TOKEN }}
                  branch: "${{ github.ref }}"
